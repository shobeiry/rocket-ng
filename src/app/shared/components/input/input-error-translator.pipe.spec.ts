import { InputErrorTranslatorPipe } from './input-error-translator.pipe';

describe('InputErrorTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new InputErrorTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
